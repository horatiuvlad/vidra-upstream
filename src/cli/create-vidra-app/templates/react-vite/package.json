{
  "name": "{{projectNameKebab}}",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "vidra build",
    "dev": "vidra dev",
    "dev:ui": "npm run dev --prefix ui",
    "dev:host:macos": "vidra run --target macos",
    "dev:host:windows": "vidra run --target windows",
    "doctor": "vidra doctor"
  },
  "devDependencies": {
    "@vidra-dev/sdk": "{{sdkVersion}}",
    "vidra-cli": "{{cliVersion}}"
  }
}
